import 'package:get/get.dart';

class NotificationBinding extends Bindings {
  @override
  void dependencies() {
    // Add NotificationController here if needed
    // Get.lazyPut<NotificationController>(() => NotificationController());
  }
}
